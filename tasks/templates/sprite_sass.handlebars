%sprites_{{title}} {
	background-image: url(../images/{{image_name}});
	background-repeat: no-repeat;
}

{{#sprites}}
.sprite_{{name}} {
	width: {{px.width}};
	height: {{px.height}};
	background-position: {{px.offset_x}} {{px.offset_y}};
	@extend %sprites_{{../title}};
}
{{/sprites}}
